import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food Scheduler Ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
